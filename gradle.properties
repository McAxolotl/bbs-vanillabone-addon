# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.16.14

# Mod Properties
mod_version=1.0.0
maven_group=mcaxolotl
archives_base_name=bbs-vanillabone-addon

# Dependencies
fabric_version=0.92.1+1.20.1

# Host (bbs-fs), from Modrinth: https://modrinth.com/mod/bbs-fs
# The version number is unique per loader on that project, so this resolves to the Fabric
# build (bbs-2.5.2-1.20.1.jar) with no loader qualifier needed. Keep it in step with the
# branch's minecraft_version.
bbs_version=2.5.2-1.20.1
